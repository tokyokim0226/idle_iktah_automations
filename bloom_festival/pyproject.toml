[project]
name = "idle-iktah-bloom-festival-automation"
version = "0.1.0"
description = "Coordinate-based Bloom Festival automation through iPhone Mirroring."
requires-python = ">=3.11"
dependencies = [
    "pynput",
    "pyautogui",
]

[tool.uv]
package = false
